#! app-dashboard-node-modules-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: app-dashboard-node-modules
    namespace: xbp-monitoring
    labels:
        com.docker.compose.service: app
        com.docker.compose.volume: dashboard_node_modules
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
