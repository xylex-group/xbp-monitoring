#! backend-backend-target-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: backend-backend-target
    namespace: xbp-monitoring
    labels:
        com.docker.compose.service: backend
        com.docker.compose.volume: backend_target
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
