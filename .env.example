# OpenTelemetry Configuration
# OTLP Exporter Settings
# Endpoint for OTLP exporter (default: http://localhost:4317)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# Protocol for OTLP exporter: grpc, http/protobuf, or http/json (default: grpc)
OTEL_EXPORTER_OTLP_PROTOCOL=grpc

# Timeout for OTLP exporter in seconds (default: 10)
OTEL_EXPORTER_OTLP_TIMEOUT=10

# Metrics Exporter Configuration
# Options: otlp, stdout, prometheus (unset = disabled)
# Set to 'prometheus' to enable Prometheus metrics endpoint
OTEL_METRICS_EXPORTER=prometheus

# Traces Exporter Configuration
# Options: otlp, stdout (unset = disabled)
# Set to 'stdout' for local development to print spans to console
OTEL_TRACES_EXPORTER=stdout

# Prometheus Exporter Settings (only used when OTEL_METRICS_EXPORTER=prometheus)
# Host for Prometheus metrics endpoint (default: localhost)
OTEL_EXPORTER_PROMETHEUS_HOST=localhost

# Port for Prometheus metrics endpoint (default: 9464)
OTEL_EXPORTER_PROMETHEUS_PORT=9464

# OpenTelemetry Resource Attributes
# Standard OpenTelemetry resource attributes (optional)
# Example: service.name=xbp-monitoring,service.version=1.0.0
OTEL_RESOURCE_ATTRIBUTES=

# Custom Environment Variables
# Add any custom environment variables here that you use in your YAML config files
# These can be referenced in xbp.yaml using: ${{ env.VAR_NAME }}
# Example:
# API_KEY=your-api-key-here
# DATABASE_URL=postgresql://localhost:5432/mydb
