# GitHub Workflow Environment Variables and Secrets
# 
# This file documents environment variables and secrets used in GitHub Actions workflows.
# Most of these are automatically provided by GitHub Actions, but some may need to be
# configured in your repository settings under Settings > Secrets and variables > Actions.
#
# Note: This file is for documentation only. Do not commit actual secrets to the repository.

# ==============================================================================
# Docker Build Workflow (.github/workflows/docker.yaml)
# ==============================================================================

# These are set automatically in the workflow, but documented here for reference:
# REGISTRY=ghcr.io
# IMAGE_NAME=${{ github.repository }}

# GitHub automatically provides this token with appropriate permissions:
# GITHUB_TOKEN (auto-provided)
#   - Used for authentication to GitHub Container Registry
#   - Permissions set in workflow: contents:read, packages:write, attestations:write, id-token:write
#   - No manual configuration needed

# ==============================================================================
# Test Workflow (.github/workflows/test.yaml)
# ==============================================================================

# Set automatically in workflow:
CARGO_TERM_COLOR=always

# ==============================================================================
# Release Workflow (.github/workflows/release.yaml)
# ==============================================================================

# GitHub automatically provides this token:
# GITHUB_TOKEN (auto-provided)
#   - Used for creating GitHub releases
#   - Permissions set in workflow: contents:write
#   - No manual configuration needed

# ==============================================================================
# Lint Workflow (.github/workflows/lint.yaml)
# ==============================================================================

# No environment variables required

# ==============================================================================
# Optional: Custom Secrets for Your Workflows
# ==============================================================================

# If you need to add custom secrets for your workflows (e.g., API keys, deployment tokens),
# configure them in GitHub repository settings:
# Settings > Secrets and variables > Actions > New repository secret
#
# Example secrets you might need:
# DOCKER_HUB_USERNAME=your-dockerhub-username
# DOCKER_HUB_TOKEN=your-dockerhub-token
# DEPLOYMENT_KEY=your-deployment-key
# SLACK_WEBHOOK_URL=your-slack-webhook-url
